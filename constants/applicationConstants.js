'use strict';

var keyMirror = require('keyMirror');

module.exports = keyMirror({
  CURRENT_STREAM: null,
  SET_CURRENT_STREAM: null,
  SET_CHANNEL_ITEMS_VIEW: null,
  SET_SETTINS_MODAL_VISIBLE: null,

  GRID_LIST: null,
  GRID: null,
  LIST: null,
});
